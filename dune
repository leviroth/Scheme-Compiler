(executable
 (name compiler)
 (libraries base stdio)
)

(ocamllex lexer)

(menhir
 (modules parser)
 (flags -la 2))

; (rule
;   (with-stdin-from prog.in (run ./compiler.exe)))

(env
  (dev
    (flags (:standard -warn-error -A))))
